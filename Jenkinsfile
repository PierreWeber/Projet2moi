pipeline {
    agent any
stages {
   stage('Connect') {
            steps {
                echo "Building.."
                sh '''
                echo "doing build stuff.."
                
                '''
                }}
}}
